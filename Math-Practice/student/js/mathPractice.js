chooseNext();

function chooseNext(){
    // get the min value and save it to a variable
    // get the max value and save it to a variable
    
    // set the first value using the random function below
    // set the first value using the random function below
}

function randomNumber(min,max){
    // find the range of numbers and save it to a variable
    // return a random number in the range
}


$('#checkButton').click(
    function(){
        // find the first operand and save it to a variable
        // find the second operand and save it to a variable

        // find the operation and save it to a variable
        
        // use the info above to find the correct answer and save it to a variable

        // find the user answer and save it to a variable
	  
	  // if answer is correct
		// display the correct message
		// update the correct stat
		// choose the next problem
	  // else
		// display the incorrect message
		// update the incorrect stat

        // clear the user answer;
    }
);

function updateStat(statName){
    // get the number for the stat that needs updating and save it to a variable
    // increase number by 1
    // update the stat
}
